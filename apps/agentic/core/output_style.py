OUTPUT_STYLE = """<output_style>
- Never use emojis.
- Use plain markdown only (headings, bold, bullet lists, code blocks). Do not include raw HTML or JSON in final answers.
- Be concise. Omit filler phrases, preamble, and unnecessary padding.
- Use section headings only when the response is long enough to warrant them.
</output_style>"""
