# Copy to .env and fill in. The real .env is gitignored.
# Data-source keys (FRED/BLS/Tiingo) are shared with the navi library.

# --- LLM provider ---------------------------------------------------------
# Provide the key matching LLM_MODEL_PROVIDER (default: openai).
OPENAI_API_KEY=your_openai_api_key_here
ANTHROPIC_API_KEY=your_anthropic_api_key_here
LLM_MODEL_PROVIDER=openai
AGENT_LLM_MODEL=gpt-4.1
# FILTER_LLM_MODEL=gpt-4.1-mini
# SCORING_LLM_MODEL=gpt-4.1-mini

# Sampling / reasoning tuning (all optional — defaults preserve current behavior):
# LLM_TEMPERATURE   — applies to models that accept it (OpenAI, Sonnet, Haiku).
#                     Opus 4.7+/Fable/Mythos ignore it (they reject sampling params).
# LLM_TEMPERATURE=0.7
# AGENT_LLM_THINKING — enable adaptive thinking on the agent (Opus 4.6+/Sonnet 4.6 only).
# AGENT_LLM_THINKING=false
# AGENT_LLM_EFFORT   — agent reasoning effort: low | medium | high | xhigh | max.
# AGENT_LLM_EFFORT=high
# LLM_MAX_TOKENS     — max output tokens (shared reasoning+answer budget when thinking
#                      is on — give it headroom). Defaults to the library default (1024).
# LLM_MAX_TOKENS=64000

# --- Data sources ---------------------------------------------------------
# FRED  — https://fred.stlouisfed.org/docs/api/api_key.html
FRED_API_KEY=your_fred_api_key_here
# BLS   — https://data.bls.gov/registrationEngine/
BLS_API_KEY=your_bls_api_key_here
# Tiingo — https://www.tiingo.com/account/api/token
TINGO_API_KEY=your_tiingo_api_key_here

# --- Web search -----------------------------------------------------------
# Tavily — https://tavily.com/
TAVILY_API_KEY=your_tavily_api_key_here

# --- GitHub (optional, for loading repos into the code RAG store) ---------
# GITHUB_API_KEY=your_github_token_here

# --- Database -------------------------------------------------------------
YADA_DB_URL=postgresql+psycopg2://user:password@localhost:5432/yada

# --- MCP data server (optional; default shown) ----------------------------
# MCP_URL=http://localhost:8080/sse

# --- Retrieval / runtime (optional) ---------------------------------------
# RAG_SCORE_THRESHOLD=0.3
# YADA_CHECKPOINT_DIR=.langgraph

# --- LangSmith tracing (optional) -----------------------------------------
# LANGCHAIN_TRACING_V2=true
# LANGCHAIN_API_KEY=your_langsmith_api_key_here
# LANGCHAIN_PROJECT=yada
